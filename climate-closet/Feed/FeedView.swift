import SwiftUI

struct FeedView: View {
    var body: some View {
        Text("this is the feed page")
            .navigationTitle("Feed")
    }
}

struct FeedView_Previews: PreviewProvider {
    static var previews: some View {
        FeedView()
    }
}
