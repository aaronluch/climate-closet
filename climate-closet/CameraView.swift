import SwiftUI

struct CameraView: View {
    var body: some View {
        Text("this is where the camera implementation goes")
            .navigationTitle("Camera")
    }
}

struct CameraView_Previews: PreviewProvider {
    static var previews: some View {
        CameraView()
    }
}

